<ul class="nav pull-right">
    <li class="dropdown" id="menu1">
        @if (Html.IsAuthenticated())
        {
            <a role="login" class="dropdown-toggle" data-toggle="dropdown" href="@Url.Content("~/logout")">Logout
                <b class="caret"></b>
            </a>
        }
        else
        {
            <a role="logout" class="dropdown-toggle" data-toggle="dropdown" href="@Url.Content("~/login")">Login
                <b class="caret"></b>
            </a>
        }
        <ul class="dropdown-menu">
            @if (Html.IsAuthenticated())
            {
                <li><a href="@Url.Content("~/logout")">Logout</a></li>                                
            }
            else
            {
                <li><a href="@Url.Content("/login")">Login</a></li>
                <li><a href="@Url.Content("/register")">Register</a></li>
            }
            <li class="divider"></li>
            <li><a href="@Url.Content("/about")">About</a></li>
        </ul>
    </li>
</ul>